<table border="1">
    <thead>
    <tr>
      <th>id</th>
      <th>attendees</th>
      <th>title</th>
      <th>startTime</th>
      <th>description</th>
      <th>location</th>
    </tr>
  </thead>

  <tbody>
        <tr>
        <td>{{event.id}}</td>
        <td>{{#each people}}<a href="../people/{{id}}">{{name}}</a><br/>{{/each}}</td>
        <td>{{event.title}}</td>
        <td>{{event.startTime}}</td>
        <td>{{event.description}}</td>
        <td><a href="../locations/{{event.location}}">{{event.location}}</a></td>
        </tr>
  </tbody>
</table>
