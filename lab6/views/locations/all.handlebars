<table border="1">
    <thead>
    <tr>
      <th>id</th>
      <th>name</th>
      <th>location</th>
    </tr>
  </thead>

  <tbody>
        {{#each locations}} 
        <tr>
        <td><a href="locations/{{id}}">{{id}}</a></td>
        <td><a href="locations/{{id}}">{{name}}</a></td>
        <td><a href="locations/{{id}}">{{location}}</a></td>
        </tr>
        {{/each}}
  </tbody>
</table>
